# HP HTTP Server 技术债跟踪

本文档记录 HP HTTP Server 跨版本、跨阶段仍需跟踪的技术债、风险、延期事项和已知缺口。它不替代 `ROADMAP.md`、`ARCHITECTURE.md`、阶段设计文档、Builder 报告或 Reviewer 报告。

本文档应在每个阶段完成后、下个阶段开始前更新。已关闭条目不得删除，应保留关闭依据，方便后续设计和审查追溯。

## 当前概况

当前项目处于准备阶段，已完成长期架构文档和路线图初始化，尚未进入 `V0.1 / S1` 代码实现。

最近完成内容：

- 初始化 `ARCHITECTURE.md`，明确 C++20、Linux、`epoll`、Reactor、HTTP/1.1 静态文件服务和 L7 Gateway 扩展边界。
- 初始化 `ROADMAP.md`，规划 `V0.1` 到 `V1.1` 的版本路线、阶段划分、禁止范围和完成标准。

下一阶段准备进入：

- `V0.1 / S1 项目骨架与基础资源封装`。

当前总体风险：

- 目前还没有代码实现，因此不存在由 Reviewer 确认的实现缺陷。
- 当前主要风险来自范围控制、性能数据代表性、后续日志/解析器/代理能力的延期决策，以及文档与实现可能脱节。
- 暂无阻塞 `V0.1 / S1` 开始的技术债。

## 阶段状态

### 准备阶段 / 架构与路线初始化

状态：已完成。

原计划目标：

建立项目长期架构、版本路线、范围边界和文档协作规范，为后续 Leader 设计、Builder 实现和 Reviewer 审查提供权威依据。

已完成内容：

- 已填充 `ARCHITECTURE.md`。
- 已填充 `ROADMAP.md`。
- 已确认项目定位为面向高性能网络岗的 Linux C++ HTTP Server。
- 已将轻量 L7 Reverse Proxy / Gateway 规划为 HTTP Server 内核稳定后的扩展方向。

待处理内容：

- 需要继续填充 `README.md`、`CHANGELOG.md` 等根文档。
- 需要为 `V0.1 / S1` 编写阶段详细设计文档。
- 需要在进入代码实现前确认构建工具链、目录结构和最小测试策略。

验证情况：

- 当前阶段为文档准备阶段，未运行代码测试。
- 已通过文档结构检查确认 `ROADMAP.md` 标题层级完整。

相关文档：

- 架构文档：`ARCHITECTURE.md`
- 路线图：`ROADMAP.md`
- 架构规范：`docs/references/ARCHITECTURE.md`
- 路线图规范：`docs/references/ROADMAP.md`

### V0.1 / S1 项目骨架与基础资源封装

状态：未开始。

原计划目标：

建立 CMake、目录结构、基础类型、日志、socket RAII 和最小启动入口，为最小可运行 HTTP Server 打基础。

已完成内容：

- 暂无。

待处理内容：

- 编写阶段设计文档。
- 明确最小目录结构和命名规范。
- 明确构建命令、测试入口和 smoke test 的初始边界。

验证情况：

- 尚未开始，暂无验证结果。

相关文档：

- 计划设计文档：`docs/leader/designs/V0.1/S1-design.md`
- 计划实现报告目录：`docs/builder/reports/V0.1/`
- 计划审查报告目录：`docs/reviewer/reports/V0.1/`

### V0.1 / S2 单线程 epoll 与连接读写

状态：未开始。

原计划目标：

实现监听 socket、非阻塞连接、`epoll` 事件循环、连接状态和短写处理。

已完成内容：

- 暂无。

待处理内容：

- 等待 `V0.1 / S1` 完成后再开始阶段设计。
- 设计时需重点确认 fd 所有权、连接关闭语义、短读短写和错误事件处理。

验证情况：

- 尚未开始，暂无验证结果。

相关文档：

- 计划设计文档：`docs/leader/designs/V0.1/S2-design.md`
- 计划实现报告目录：`docs/builder/reports/V0.1/`
- 计划审查报告目录：`docs/reviewer/reports/V0.1/`

### V0.1 / S3 最小 HTTP 静态文件服务

状态：未开始。

原计划目标：

实现基础 HTTP 解析、路径安全、静态文件响应、错误码和 smoke test。

已完成内容：

- 暂无。

待处理内容：

- 等待 `V0.1 / S2` 完成后再开始阶段设计。
- 设计时需重点确认路径穿越防护、错误码边界、响应格式和 curl 验证用例。

验证情况：

- 尚未开始，暂无验证结果。

相关文档：

- 计划设计文档：`docs/leader/designs/V0.1/S3-design.md`
- 计划实现报告目录：`docs/builder/reports/V0.1/`
- 计划审查报告目录：`docs/reviewer/reports/V0.1/`

## 技术债条目

### TD-001 WSL2 性能数据代表性有限

状态：持续跟踪。

影响范围：

`V0.5`、`V0.6`、`V1.0`，影响压测报告、README 性能摘要和简历表述。

发现来源：

Leader 规划。

问题描述：

项目主要开发环境可以使用 WSL2，但 WSL2 的网络栈、虚拟化层和系统观测能力与原生 Linux 服务器不完全一致。后续如果直接使用 WSL2 压测结果作为最终性能结论，可能导致数据代表性不足。

风险：

如果不区分开发环境和性能验证环境，README 或简历中的 QPS、延迟、CPU 热点结论可能被面试追问时暴露为不可复现或不具备代表性。

处理建议：

`V0.5` 建立压测基线时可以先记录 WSL2 数据，但必须标注环境。`V0.6` 或 `V1.0` 前建议在原生 Linux、云服务器或更接近真实环境的机器上复测关键数据。

当前决定：

接受该风险并持续跟踪，不阻塞 `V0.1` 到 `V0.4` 开发。

相关证据：

- 路线图：`ROADMAP.md`
- 架构文档：`ARCHITECTURE.md`

更新记录：

- `2026-05-21`：新增，原因是项目开发环境优先支持 WSL2，但性能结论需要更严格的环境说明。

### TD-002 异步日志方案延期到性能阶段决策

状态：接受延期。

影响范围：

`V0.1` 到 `V0.5`，影响日志模块、热路径阻塞风险和性能优化叙事。

发现来源：

Leader 规划。

问题描述：

`V0.1` 到 `V0.4` 需要优先完成网络模型、HTTP 解析和并发资源治理。异步日志虽然是高性能服务器的重要能力，但过早实现会增加线程、队列、刷盘、丢弃策略和关闭流程复杂度。

风险：

如果前期日志能力过弱，调试和审查信息可能不足；如果过早实现完整异步日志，又可能干扰 Reactor、连接管理和 HTTP 状态机主线。

处理建议：

前期保留简单同步日志接口，避免在热路径输出大量日志。`V0.5 / S2` 正式决定自研简化异步日志或采用其他受控方案，并记录取舍。

当前决定：

接受延期到 `V0.5 / S2`，前期不扩大日志复杂度。

相关证据：

- `ROADMAP.md` 中 `V0.5 / S2 异步日志与 IO 路径减负`
- `ARCHITECTURE.md` 中 `base` 模块职责

更新记录：

- `2026-05-21`：新增，原因是日志能力需要服务性能目标，但不应干扰早期网络模型落地。

### TD-003 HTTP Parser 范围需防止过度扩张

状态：持续跟踪。

影响范围：

`V0.3` 及后续 HTTP 协议层。

发现来源：

Leader 规划。

问题描述：

HTTP/1.1 协议细节较多，包括 request body、chunked、pipelining、Range、缓存协商等。项目当前目标是高性能网络服务器，不是完整 Web 框架或完整 HTTP 兼容性实现。如果 Parser 范围失控，会拖慢核心网络能力建设。

风险：

过度扩张会导致阶段延期、测试矩阵膨胀、协议边界难以审查，并稀释 Reactor、连接管理、资源治理等网络岗核心亮点。

处理建议：

`V0.3` 只聚焦请求行、Header、请求边界、Header 上限、keep-alive 和异常请求。request body、chunked、pipelining、Range 等能力必须由后续路线或阶段设计明确后再进入实现。

当前决定：

持续跟踪，`V0.3` 阶段设计必须明确 Parser 禁止范围。

相关证据：

- `ROADMAP.md` 中 `V0.3 HTTP 状态机与连接复用`
- `ARCHITECTURE.md` 中 `http` 模块职责

更新记录：

- `2026-05-21`：新增，原因是 HTTP Parser 是后续最容易范围膨胀的模块之一。

### TD-004 L7 Gateway 扩展存在范围膨胀风险

状态：持续跟踪。

影响范围：

`V1.1` 及后续代理能力。

发现来源：

Leader 规划。

问题描述：

L7 Gateway 能力可以降低普通 HTTP Server 项目的同质化，但网关方向容易扩张到服务发现、动态配置、限流、熔断、灰度发布、TLS 终止等生产级能力。当前项目目标仍是高性能 HTTP Server 内核，不应过早转向完整网关平台。

风险：

如果代理能力过早或过度扩张，会削弱项目主线，导致文档、测试、性能分析和实现复杂度成倍增加。

处理建议：

`V1.1` 仅实现轻量路径路由、upstream 选择、HTTP 转发、超时和错误处理。任何生产级网关治理能力必须作为远期方向，不进入 `V1.1` 默认范围。

当前决定：

持续跟踪，不阻塞 `V0.1` 到 `V1.0`。进入 `V1.1` 前必须重新确认用户目标。

相关证据：

- `ROADMAP.md` 中 `V1.1 轻量 L7 Gateway 扩展`
- `ARCHITECTURE.md` 中 `proxy` 模块职责和架构约束

更新记录：

- `2026-05-21`：新增，原因是 L7 Gateway 是项目差异化方向，但必须控制边界。

### TD-005 文档与实现可能在阶段推进中脱节

状态：持续跟踪。

影响范围：

所有版本和阶段。

发现来源：

Leader 规划。

问题描述：

项目采用 Leader 设计、Builder 实现、Reviewer 审查的文档闭环。随着代码实现推进，`ARCHITECTURE.md`、`ROADMAP.md`、阶段设计、实现报告、审查报告和 README 之间可能出现不同步。

风险：

如果文档与实现脱节，Builder 可能按过期设计实现，Reviewer 可能按错误边界审查，最终 README 和简历表述也可能与真实能力不一致。

处理建议：

每个阶段完成后必须由 Leader 根据 Builder 报告和 Reviewer 报告更新本文件、必要时更新 `ARCHITECTURE.md`、`ROADMAP.md`、`README.md` 和 `CHANGELOG.md`。Reviewer 发现文档不一致时，应作为审查问题记录。

当前决定：

持续跟踪，从 `V0.1 / S1` 开始执行阶段后更新。

相关证据：

- `AGENTS.md`
- `docs/leader/GUIDE.md`
- `ROADMAP.md`
- `ARCHITECTURE.md`

更新记录：

- `2026-05-21`：新增，原因是项目强调文档闭环，必须提前把文档漂移作为长期风险跟踪。

## 风险观察

### RO-001 早期目录结构尚未由代码验证

观察到什么：

当前 `ARCHITECTURE.md` 和 `ROADMAP.md` 已规划 `app`、`include`、`src`、`tests`、`benchmark` 等目录，但代码尚未实现，目录边界还没有被实际构建和测试验证。

目前为什么还不算技术债：

项目仍处于准备阶段，目录尚未落地并不代表实现缺陷。

升级为技术债的条件：

如果 `V0.1 / S1` 完成后目录结构与架构文档不一致，且没有在阶段报告中解释原因，应升级为技术债。

### RO-002 性能目标尚未量化

观察到什么：

路线图已经规划 wrk 压测、P95/P99 和性能报告，但当前没有硬性 QPS 或延迟目标。

目前为什么还不算技术债：

项目尚未实现最小服务器，没有基线前不适合承诺绝对性能指标。

升级为技术债的条件：

如果 `V0.5` 完成后仍没有可复现压测基线和环境说明，应升级为技术债。

### RO-003 L7 Gateway 与独立 L4LB 项目的边界需持续区分

观察到什么：

用户已有单独 L4LB 项目，本项目后续只规划应用层 L7 Gateway 扩展。

目前为什么还不算技术债：

当前架构和路线图已经明确禁止 L4LB、XDP、DPDK 和内核旁路能力。

升级为技术债的条件：

如果后续阶段设计开始引入 L4 转发、NAT、IPVS 或内核网络能力，应升级为范围膨胀技术债。

## 下一阶段检查点

进入 `V0.1 / S1` 前，Leader 需要检查：

- 确认 `V0.1 / S1` 只做项目骨架、基础资源封装和最小启动入口，不提前实现 HTTP Server 完整能力。
- 确认 C++ 标准、构建系统、目录结构和命名风格与 `ARCHITECTURE.md` 一致。
- 确认日志只保留简单接口，不提前实现异步日志。
- 确认 socket RAII、不可拷贝资源和错误处理边界写入阶段设计。
- 确认 Builder 需要产出实现报告，Reviewer 需要产出审查报告。
- 确认 README 在出现可运行命令后同步更新。

## 关闭规则

技术债关闭时不得删除条目。关闭时应将状态改为 `已解决` 或 `不再适用`，并补充关闭依据。

可接受的关闭依据包括：

- Reviewer 审查报告确认问题已解决。
- 测试命令和结果证明问题已修复。
- 阶段设计明确取消相关需求。
- 用户明确接受风险或调整路线。
- 后续版本实现已经使该问题不再成立。

关闭记录必须写入对应条目的更新记录中，并保留相关报告、测试命令或文档路径。

## 变更记录

- `2026-05-21`：初始化技术债跟踪文档，记录准备阶段状态，新增 TD-001 至 TD-005，并建立下一阶段检查点。
